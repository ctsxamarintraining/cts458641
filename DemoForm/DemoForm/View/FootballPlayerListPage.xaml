<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="DemoForm.FootballPlayerListPage">
	<ContentPage.ToolbarItems>

            <ToolbarItem Name="Add Player" Activated="OnClick"  Order="Primary" Priority="0" />
      

    </ContentPage.ToolbarItems>


	<ContentPage.Content>

	 <ListView x:Name="FootballPlayerPages1" ItemsSource="{Binding Football_Player}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout BackgroundColor="#eee"
                        Orientation="Vertical">
            
                                 <Label Text="{Binding cName}"
                                TextColor="#f35e20" />
                                 <Label Text="{Binding country}"
                                TextColor="#f35e20" />
                                <Label Text="{Binding descriptiondet}"
                                TextColor="#f35e20" />


                               </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>



	</ContentPage.Content>
</ContentPage>